services:
  swagger-ui:
    image: swaggerapi/swagger-ui:v5.32.14
    container_name: identity-swagger-ui
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      SWAGGER_JSON: /openapi/openapi.yml
    volumes:
      - ./docs/openapi.yml:/openapi/openapi.yml:ro